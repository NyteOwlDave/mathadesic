<style>
body {
    font-size : 24pt;
    text-align : center;
}
code {
    filter : drop-shadow( 0px 0px 11px black );
    display : block;
    font-size : 14pt;
    color : darkblue;
}
</style>

# Chord Area

```
A = r² * (cos⁻¹((r-h)/r) - ((r-h)/r) * √(2rh - h²))

A = Area
r = radius
h = height of sagitta
```

The `sagitta` is the line representing the shortest
distance form the circle's origin to the line that
intersects with the circle. This line will aways
always be perpendicular to the line of intersection.

$$
$$


